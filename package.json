{
  "name": "petserve-prototype",
  "version": "0.3.0",
  "private": true,
  "description": "Local PetServe appointment and payment-recording prototype",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
